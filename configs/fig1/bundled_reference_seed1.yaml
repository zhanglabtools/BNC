experiment: fig1_bundled_reference
protocol: fig1_pca
formal: true
device: cuda
train_fraction: 0.7
grid:
  architectures: [mlp]
  moduli: [97]
  seeds: [1]
model:
  architecture: mlp
  embedding_dim: 256
  hidden_dim: 128
training:
  epochs: 10000
  lr: 0.001
  weight_decay: 1.0
  betas: [0.9, 0.98]
  scheduler: null

